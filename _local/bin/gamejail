#!/bin/bash -e

if [[ $# < 2 ]]; then
    echo "Usage: $0 DIRECTORY EXECUTABLE [ARGUMENTS]"
    exit 1
fi

directory="$1"
shift

firejail --profile=~/.config/firejail/games.profile --private="$directory" -- "$@"
